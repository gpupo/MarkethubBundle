sudo: false
language: php
os:
  - linux
php:
  - 7.2
  - nightly
matrix:
  allow_failures:
    - php: nightly
before_script:
  - composer self-update
  - composer install --prefer-dist
script: vendor/bin/simple-phpunit
notifications:
  email: false
